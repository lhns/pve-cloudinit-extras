#!/bin/sh
set -e
case "$1" in
    remove|upgrade|deconfigure)
        pve-cloudinit-extras-patch api remove || true
        pve-cloudinit-extras-patch gui remove || true
        ;;
esac
#DEBHELPER#
exit 0
