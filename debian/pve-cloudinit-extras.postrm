#!/bin/sh
# Generated cix-*-vendor.yaml snippets and cicustom values are deliberately left alone.
set -e
if [ "$1" = purge ]; then
    rm -rf /var/lib/pve-cloudinit-extras /etc/pve-cloudinit-extras.conf
fi
#DEBHELPER#
exit 0
